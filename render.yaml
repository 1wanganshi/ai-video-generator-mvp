services:
  - type: web
    name: ai-video-generator-mvp
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    disk:
      name: app-storage
      mountPath: /opt/render/project/src/storage
      sizeGB: 1
